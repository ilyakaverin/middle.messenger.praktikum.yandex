include ../../components/chat-container/index.pug

+chat-container('chat-container hidden', 'chat_container')
    #{chat_list}
        +chat-container('chat-container_inner')
            #{chat_messages}
            #{chat_input}

